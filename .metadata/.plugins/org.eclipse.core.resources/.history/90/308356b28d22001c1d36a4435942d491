package es.cfc.business;

import es.cfc.models.Estado;
import es.cfc.models.Tarea;

public class GestionTareas {

	public Tarea crearTarea(int id, String description) {
		return new Tarea(id, description, Estado.NUEVA);
	}
	
	public String mostrarTarea(Tarea tarea) {
		return tarea.toString();
	}
		
	public Tarea procesarTarea(Tarea tarea) {
		Estado estado = tarea.getEstado();
		
		if (estado == Estado.EN_PROCESO) {
			tarea.setEstado(Estado.FINALIZADA);
		} else if (estado == Estado.NUEVA) {
			tarea.setEstado(Estado.EN_PROCESO);
		} else {
			tarea.setEstado(Estado.EN_PROCESO);
		}
		return tarea;
	}
	
}
